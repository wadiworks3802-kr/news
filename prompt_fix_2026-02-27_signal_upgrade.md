# PROMPT-2026-02-27 (실행형): KR 주식 특화 AI 어시 고도화 + 배포 검증

## 목적
- 국내주식(KR) 중심의 추천/보조 시스템을 현재 구현 가능한 범위에서 안정화한다.
- 글로벌 주요국 뉴스가 국내주식에 미치는 영향을 반영하되, 실주문 자동화는 이번 범위에서 제외한다.
- 서버 배포 후 실제 동작 검증을 통과해야 완료로 본다.

## 최종 목표 정렬
- 지금 단계: 수집 -> 분석 -> 추천 -> 설명 가능한 AI 어시스트
- 다음 단계(후속): UI 대규모 고도화(토스/네이버증권 수준), 자동매매 승인/실행 고도화
- 이번 단계 제외: 승인 없는 실주문 자동 실행

## 원인 분석 요약
1. 바 데이터 수집이 `1m`, `3개` 중심이라 과거 맥락(1d/7d/중기) 반영이 사실상 어려웠다.
2. 전략 엔진은 `H1`/`D1` 중심 분석인데 저장 키(`1h/H1`, `1d/D1`)가 섞여 조회 실패가 발생했다.
3. 전략 상태 문구가 정적 문자열 위주라 상세 설명이 종목별로 비슷해 보였다.
4. 시그널 DTO/UI에 시세/등락률 필드가 제한적이라 종목별 차이를 체감하기 어려웠다.
5. 단타 패널이 비면 완전 빈 화면으로 보이는 구간이 발생했다.

## 구현 요구사항 (이번 사이클 필수)
1. 수집 고도화
- 배치 바 수집을 `1m + 1h + D1` 다중 타임프레임으로 확장한다.
- 기본 수집량을 상향한다: `1m=240`, `H1=240`, `D1=365`.
- timeframe 정규화(`1h/H1`, `1d/D1`)를 저장/조회 모두에 적용한다.

2. 추천/점수 고도화
- `StockSignalService`에 최신 등락률 + 1일/7일 수익률 기반 모멘텀을 반영한다.
- 추천 reason에 뉴스/시세/모멘텀 기여치를 함께 표기한다.
- 단타 패널 공백 시 최근 데이터 기반 fallback을 제공한다.

3. 설명 품질 고도화
- Scalp/Swing/Chart/Discovery `state_reason`을 종목 지표 기반 동적 문구로 생성한다.
- 전략 요약에 동적 state reason을 병합한다.

4. 표시 고도화
- `TradingSignalViewDto`에 `last_price/change_pct/volume/quote_age_seconds/quote_provider`를 포함한다.
- 웹 패널에서 시세/등락률/provider를 노출한다.
- 등락률 단위 혼선(0.01 vs 1.0)을 보정한다.

## 배포/검증 게이트 (통과 기준)
- 로컬: `:api:test`, `:batch:compileJava`, `build` 성공
- 서버 배포 후:
  - 서비스 상태 `active` (`gnd-api`, `gnd-web`, `gnd-batch`, `gnd-h2`)
  - `/actuator/health` 200(또는 운영정책상 허용된 degraded 상태)
  - 핵심 API 200: 뉴스/전략패널/어시대시보드/시그널상세
  - 전략패널 응답에서 종목별 수치/사유가 동일값으로 수렴하지 않음
  - 단타 패널이 빈 배열만 반복되는 상태가 아님
- 실패 항목은 원인/복구 후 재검증한다.

## 보고 형식
- 변경 파일/기능 요약
- 로컬 테스트 결과
- 서버 배포 로그 핵심
- 스모크 테스트 결과
- 남은 리스크와 다음 단계
